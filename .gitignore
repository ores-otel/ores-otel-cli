/target
/node_modules
/build
/.dart_tool
/coverage
.DS_Store
.env
.env.*
!.env.example
*.sqlite

# CLI application: Cargo.lock is tracked for reproducible exact-head builds.

# Repository-local temporary data and authorized Git worktrees
tmp
temp
tmp/worktrees
temp/worktrees

# Sanctioned scratch locations - AGENTS.md requires both, trailing-slash form
tmp/
temp/

# machine-local symlink to ORESoftware/my-ai AGENTS.md; never committed
.ores/
